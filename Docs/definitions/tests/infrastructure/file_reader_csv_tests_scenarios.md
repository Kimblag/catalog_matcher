# Escenarios de tests – FileReaderCSV

| Caso de test                                | Escenario                                    | Setup (mocks / fixtures)                          | Entrada              | Comportamiento esperado                                           | Resultado esperado                          |
| ------------------------------------------ | -------------------------------------------- | ------------------------------------------------- | ------------------- | ---------------------------------------------------------------- | ------------------------------------------ |
| read_catalog_when_file_exists_should_read  | Archivo CSV válido con varias filas          | Crear archivo CSV temporal con datos             | `file_path`         | Abre el archivo y convierte cada fila en dict                     | Lista de dict con contenido del CSV         |
| read_catalog_when_file_does_not_exist      | Archivo no existe                            | Ningún archivo                                   | `file_path`         | Detecta que no existe y retorna lista vacía                       | `[]`                                        |
| read_catalog_when_file_is_empty             | Archivo existe pero vacío                    | Crear archivo CSV vacío                           | `file_path`         | Lee el archivo, devuelve lista vacía                               | `[]`                                        |
| read_requirements_when_file_exists_should_read | Archivo CSV válido con varias filas          | Crear archivo CSV temporal con datos             | `file_path`         | Igual que `read_catalog`, pero usando `read_requirements`          | Lista de dict con contenido del CSV         |
| read_requirements_when_file_does_not_exist | Archivo de requerimientos no existe         | Ningún archivo                                   | `file_path`         | Detecta que no existe y retorna lista vacía                       | `[]`                                        |
| read_requirements_when_file_is_empty        | Archivo existe pero vacío                    | Crear archivo CSV vacío                           | `file_path`         | Lee el archivo, devuelve lista vacía                               | `[]`                                        |
| read_file_should_return_dicts               | Cada fila se transforma en dict normal      | Archivo CSV con filas                            | `_read_file(Path)`  | Convierte cada fila en dict para evitar OrderedDict               | `[dict(row1), dict(row2), ...]`            |
| read_file_should_fail_on_non_csv           | Archivo no es CSV válido                     | Archivo con contenido no CSV                     | `_read_file(Path)`  | Lanza excepción de `csv.Error`                                    | Excepción `csv.Error`                        |
